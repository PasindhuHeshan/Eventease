RewriteEngine On

# accept loading of actual files and dictionaries
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

#send everything else to index.php
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]